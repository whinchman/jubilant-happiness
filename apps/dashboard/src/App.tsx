import { APP_NAME } from "@todoer/shared";

export function App() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "2rem" }}>
      <h1>{APP_NAME} — Dashboard</h1>
      <p>Big-screen board view. Phase 0: skeleton is up.</p>
    </main>
  );
}
